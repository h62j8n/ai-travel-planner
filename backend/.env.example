APP_ENV=development
CORS_ORIGINS=["http://localhost:5173"]

# OpenRouter (backend-only, never exposed to the client — PRD §8.2 / §11)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free
